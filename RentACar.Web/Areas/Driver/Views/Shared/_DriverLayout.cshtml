@{
    var title = ViewData["Title"] ?? "Driver Portal";
}
<!DOCTYPE html>
<html class="dark" lang="en">
<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>@title - Driver</title>
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&amp;display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&amp;display=swap" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <script id="tailwind-config">
        tailwind.config = {
            darkMode: "class",
            theme: {
                extend: {
                    colors: {
                        primary: "#d4af35",
                        "primary-hover": "#c29f2b",
                        "background-light": "#f8f8f5",
                        "background-dark": "#0c0b08",
                        "surface-dark": "#1d1b14",
                        "surface-darker": "#14120b"
                    },
                    fontFamily: {
                        display: "Manrope"
                    },
                    borderRadius: {
                        DEFAULT: "0.25rem",
                        lg: "0.5rem",
                        xl: "0.75rem",
                        full: "9999px"
                    }
                }
            }
        };
    </script>
    <link rel="stylesheet" href="~/css/AdminDashboard.css" />
</head>
<body class="bg-background-light dark:bg-background-dark text-slate-900 dark:text-white font-display overflow-hidden antialiased">
    <div class="flex h-screen w-full">
        <partial name="_Sidebar" />

        <main class="flex-1 flex flex-col h-full overflow-hidden relative lg:ml-[280px]">
            <header class="h-16 flex items-center justify-between px-6 lg:px-10 py-3 bg-surface-darker/80 backdrop-blur-md border-b border-white/5 shrink-0 z-20">
                <div class="flex items-center gap-4 text-white">
                    <button class="md:hidden text-white" onclick="toggleSidebar()">
                        <span class="material-symbols-outlined">menu</span>
                    </button>
                    <div>
                        <p class="text-sm text-white/60">Driver Portal</p>
                        <h1 class="text-xl font-semibold text-white">@title</h1>
                    </div>
                </div>
                <div class="flex items-center gap-3">
                    <span class="badge bg-primary text-black px-3 py-1 rounded-full text-xs font-semibold">Driver</span>
                </div>
            </header>

            <section class="flex-1 overflow-y-auto px-6 lg:px-10 py-6">
                @RenderBody()
            </section>
        </main>
    </div>

    @RenderSection("Scripts", required: false)
</body>
</html>
