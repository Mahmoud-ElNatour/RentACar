@model RentACar.Application.DTOs.EmployeeDto
<<<<<<< HEAD

<div class="modal fade" id="employeeFormModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content bg-[#141414] border border-white/10 shadow-2xl rounded-2xl">
            <!-- Header -->
            <div class="modal-header border-b border-white/5 p-5">
                <h5 class="text-xl font-bold text-white tracking-tight flex items-center gap-2">
                    <span class="material-symbols-outlined text-[#d4af37]">
                        @(Model.EmployeeId == 0 ? "person_add" : "manage_accounts")
                    </span>
                    @((Model.EmployeeId == 0) ? "Add Employee" : "Edit Employee")
                </h5>
                <button type="button" class="text-gray-400 hover:text-white transition-colors" data-bs-dismiss="modal">
                    <span class="material-symbols-outlined text-2xl">close</span>
                </button>
            </div>
            
            <!-- Body -->
            <div class="modal-body p-6">
                <form id="employeeForm" class="space-y-4">
                    <input type="hidden" asp-for="EmployeeId" />
                    
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                         <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase mb-1.5">Full Name</label>
                            <input asp-for="Name" class="w-full bg-[#0a0a0a] border border-white/10 rounded-xl px-4 py-2.5 text-white focus:outline-none focus:border-[#d4af37]/50 focus:ring-1 focus:ring-[#d4af37]/50 placeholder-gray-600" placeholder="e.g. Alice Smith" required />
                        </div>
                        <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase mb-1.5">Email Address</label>
                            <input asp-for="Email" class="w-full bg-[#0a0a0a] border border-white/10 rounded-xl px-4 py-2.5 text-white focus:outline-none focus:border-[#d4af37]/50 focus:ring-1 focus:ring-[#d4af37]/50 placeholder-gray-600" placeholder="alice@example.com" required readonly="@(Model.EmployeeId != 0 ? "readonly" : null)" />
                            <span id="empEmailError" class="text-red-500 text-xs mt-1 hidden">Please enter a valid email address.</span>
                        </div>
                    </div>

                    <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                        <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase mb-1.5">Phone Number</label>
                            <input asp-for="PhoneNumber" class="w-full bg-[#0a0a0a] border border-white/10 rounded-xl px-4 py-2.5 text-white focus:outline-none focus:border-[#d4af37]/50 focus:ring-1 focus:ring-[#d4af37]/50 placeholder-gray-600" placeholder="+1 234..." />
                        </div>
                         <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase mb-1.5">Monthly Salary ($)</label>
                            <input asp-for="Salary" type="number" step="0.01" class="w-full bg-[#0a0a0a] border border-white/10 rounded-xl px-4 py-2.5 text-white focus:outline-none focus:border-[#d4af37]/50 focus:ring-1 focus:ring-[#d4af37]/50 placeholder-gray-600" placeholder="0.00" />
                        </div>
                    </div>

                    @if (Model.EmployeeId == 0)
                    {
                        <div>
                            <label class="block text-xs font-medium text-gray-500 uppercase mb-1.5">Initial Password</label>
                            <input type="password" name="Password" class="w-full bg-[#0a0a0a] border border-white/10 rounded-xl px-4 py-2.5 text-white focus:outline-none focus:border-[#d4af37]/50 focus:ring-1 focus:ring-[#d4af37]/50 placeholder-gray-600" required />
                            <span id="empPasswordError" class="text-red-500 text-xs mt-1 hidden">Password must be 8+ chars (Upper, Lower, Number, Symbol).</span>
                             <p class="text-xs text-gray-500 mt-1">Must contain uppercase, lowercase, number, and symbol.</p>
                        </div>
                    }

                    <div>
                        <label class="block text-xs font-medium text-gray-500 uppercase mb-1.5">Address</label>
                         <textarea asp-for="Address" rows="2" class="w-full bg-[#0a0a0a] border border-white/10 rounded-xl px-4 py-2.5 text-white focus:outline-none focus:border-[#d4af37]/50 focus:ring-1 focus:ring-[#d4af37]/50 placeholder-gray-600 resize-none" placeholder="Residential Address"></textarea>
                    </div>

                    <div class="flex items-center gap-3 pt-2">
                         <label class="flex items-center gap-3 cursor-pointer group">
                             <div class="relative">
                                <input asp-for="IsActive" type="checkbox" class="peer sr-only" />
                                <div class="w-10 h-6 bg-white/10 rounded-full peer-checked:bg-green-500/50 transition-colors"></div>
                                <div class="absolute left-1 top-1 w-4 h-4 bg-gray-400 rounded-full transition-transform peer-checked:translate-x-4 peer-checked:bg-white"></div>
                            </div>
                            <span class="text-sm text-gray-300 group-hover:text-white transition-colors">Active Account</span>
                        </label>
                    </div>

                    <!-- Footer -->
                    <div class="pt-4 border-t border-white/5 flex gap-3">
                         <button type="submit" class="flex-1 bg-[#d4af37] hover:bg-[#b5952f] text-black font-semibold py-2.5 rounded-xl transition-all shadow-[0_0_15px_rgba(212,175,55,0.3)]">
                            @(Model.EmployeeId == 0 ? "Create Employee" : "Save Changes")
                        </button>
                         <button type="button" class="flex-1 bg-white/5 hover:bg-white/10 text-white font-medium py-2.5 rounded-xl transition-colors border border-white/10" data-bs-dismiss="modal">
                            Cancel
                        </button>
                    </div>
=======
<div class="modal fade" id="employeeFormModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content dark-modal">
            <div class="modal-header">
                <h5 class="modal-title">@((Model.EmployeeId == 0) ? "Add Employee" : "Edit Employee")</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="employeeForm">
                    <input type="hidden" asp-for="EmployeeId" />
                    <div class="mb-3">
                        <label class="form-label">Roles</label>
                        <div class="d-flex flex-wrap gap-3">
                            @foreach (var role in (IEnumerable<string>)(ViewBag.Roles ?? new List<string>()))
                            {
                                var isChecked = Model.Roles != null && Model.Roles.Contains(role, StringComparer.OrdinalIgnoreCase);
                                <div class="form-check">
                                    <input class="form-check-input role-checkbox" type="checkbox" value="@role" id="role_@role" @(isChecked ? "checked" : "") />
                                    <label class="form-check-label" for="role_@role">@role</label>
                                </div>
                            }
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Name</label>
                        <input asp-for="Name" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Email</label>
                        <input asp-for="Email" class="form-control" />
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Phone Number</label>
                        <input asp-for="PhoneNumber" class="form-control" />
                    </div>
                    @if (Model.EmployeeId == 0)
                    {
                        <div class="mb-3">
                            <label class="form-label">Password</label>
                            <input type="password" name="Password" class="form-control" />
                        </div>
                    }
                    <div class="mb-3">
                        <label class="form-label">Salary</label>
                        <input asp-for="Salary" class="form-control" />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Address</label>
                        <input asp-for="Address" class="form-control" />
                    </div>
                    <div class="form-check mb-3">
                        <input asp-for="IsActive" class="form-check-input" />
                        <label class="form-check-label" asp-for="IsActive">Active</label>
                    </div>
                    <div id="driverSection" class="mt-4 p-3 border rounded bg-dark">
                        <h6 class="text-warning mb-3">Driver Details</h6>
                        <div class="row g-3">
                            <div class="col-md-6">
                                <label class="form-label">Driver Code</label>
                                <input asp-for="DriverCode" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Full Name</label>
                                <input asp-for="DriverFullName" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Phone</label>
                                <input asp-for="DriverPhone" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Email</label>
                                <input asp-for="DriverEmail" class="form-control" />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Rating</label>
                                <input asp-for="DriverRating" class="form-control" />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">License Number</label>
                                <input asp-for="DriverLicenseNumber" class="form-control" />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">License Expiry</label>
                                <input asp-for="DriverLicenseExpiry" type="date" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Languages</label>
                                <input asp-for="DriverLanguages" class="form-control" />
                            </div>
                            <div class="col-md-6">
                                <label class="form-label">Is Active</label>
                                <input asp-for="DriverIsActive" type="checkbox" class="form-check-input ms-2" />
                            </div>
                            <div class="col-12">
                                <label class="form-label">Notes</label>
                                <textarea asp-for="DriverNotes" class="form-control" rows="2"></textarea>
                            </div>
                            @if (Model.DriverCreatedAt.HasValue)
                            {
                                <div class="col-md-6">
                                    <label class="form-label">Created At</label>
                                    <input class="form-control" value="@Model.DriverCreatedAt.Value.ToString("g")" readonly />
                                </div>
                            }
                            @if (Model.DriverUpdatedAt.HasValue)
                            {
                                <div class="col-md-6">
                                    <label class="form-label">Updated At</label>
                                    <input class="form-control" value="@Model.DriverUpdatedAt.Value.ToString("g")" readonly />
                                </div>
                            }
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary">@(Model.EmployeeId == 0 ? "Add" : "Save")</button>
>>>>>>> Mahmoud-V3
                </form>
            </div>
        </div>
    </div>
</div>
<script>
<<<<<<< HEAD
    document.getElementById('employeeForm').addEventListener('submit', async function (e) {
        e.preventDefault();
        const form = e.target;
        const btn = form.querySelector('button[type="submit"]');
        const originalText = btn.textContent;
        
        // --- Validation Start ---
        const f = form.elements;
        const email = f['Email'].value;
        const passwordInput = f['Password']; 
        const employeeId = parseInt(form.querySelector('[name="EmployeeId"]').value || '0');
        const emailError = document.getElementById('empEmailError');
        const passwordError = document.getElementById('empPasswordError');

        // Reset
        if(emailError) emailError.classList.add('hidden');
        if(passwordError) passwordError.classList.add('hidden');

        let isValid = true;

        // Email Validation
        const emailRegex = /^[^\s@@]+@@[^\s@@]+\.[^\s@@]+$/;
        if (!emailRegex.test(email)) {
             if(emailError) emailError.classList.remove('hidden');
             isValid = false;
        }

        // Password Validation (New Employee Only)
        if (employeeId === 0 && passwordInput) {
            const password = passwordInput.value;
            const passwordRegex = /^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[\W_]).{8,}$/;
            if (!passwordRegex.test(password)) {
                if(passwordError) passwordError.classList.remove('hidden');
                isValid = false;
            }
        }
        
        if(!isValid) {
            btn.disabled = false;
            btn.textContent = originalText;
            return;
        }
        // --- Validation End ---

        btn.disabled = true;
        btn.innerHTML = '<span class="material-symbols-outlined animate-spin text-sm">sync</span> Processing...';

        const data = {
            employeeId: employeeId,
            name: f['Name'].value,
            email: email,
            username: email,
            phoneNumber: f['PhoneNumber'].value,
            salary: f['Salary'].value ? parseFloat(f['Salary'].value) : null,
            address: f['Address'].value,
            isActive: form.querySelector('[name="IsActive"]').checked
        };
       
        try {
            let url = '/api/Employee';
            let method = 'POST';
            const isUpdate = Boolean(data.employeeId); 

            if (isUpdate) {
                url += '/' + data.employeeId;
                method = 'PUT';
            } else {
                 data.password = passwordInput.value;
            }

=======
    function getSelectedRoles() {
        return Array.from(document.querySelectorAll('.role-checkbox:checked')).map(cb => cb.value);
    }

    function toggleDriverSection() {
        const roles = getSelectedRoles();
        const driverSection = document.getElementById('driverSection');
        const show = roles.some(role => role.toLowerCase() === 'driver');
        driverSection.style.display = show ? 'block' : 'none';
    }

    document.querySelectorAll('.role-checkbox').forEach(cb => {
        cb.addEventListener('change', toggleDriverSection);
    });
    toggleDriverSection();

    document.getElementById('employeeForm').addEventListener('submit', async function (e) {
        e.preventDefault();
        const form = e.target;
        const f = form.elements;
        const roles = getSelectedRoles();
        const data = {
            employeeId: parseInt(form.querySelector('[name="EmployeeId"]')?.value || '0'),
            name: f['Name']?.value,
            email: f['Email']?.value,
            username: f['Email']?.value,
            phoneNumber: f['PhoneNumber']?.value,
            salary: parseFloat(f['Salary']?.value || '0'),
            address: f['Address']?.value,
            isActive: form.querySelector('[name="IsActive"]')?.checked ?? false,
            roles: roles,
            driverCode: f['DriverCode']?.value,
            driverFullName: f['DriverFullName']?.value,
            driverPhone: f['DriverPhone']?.value,
            driverEmail: f['DriverEmail']?.value,
            driverRating: f['DriverRating']?.value ? parseFloat(f['DriverRating']?.value) : null,
            driverLicenseNumber: f['DriverLicenseNumber']?.value,
            driverLicenseExpiry: f['DriverLicenseExpiry']?.value || null,
            driverLanguages: f['DriverLanguages']?.value,
            driverNotes: f['DriverNotes']?.value,
            driverIsActive: form.querySelector('[name="DriverIsActive"]')?.checked ?? false
            };
        let url = '/api/Employee';
        let method = 'POST';
        const isUpdate = Boolean(data.employeeId);
        if (isUpdate) {
            url += '/' + data.employeeId;
            method = 'PUT';
        } else {
            data.password = f['Password']?.value;
        }
        try {
>>>>>>> Mahmoud-V3
            const response = await fetch(url, {
                method: method,
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify(data)
            });

            if (!response.ok) {
                let message = 'Failed to save employee.';
<<<<<<< HEAD
                 try {
                    const payload = await response.json();
                    if (payload && payload.message) message = payload.message;
                } catch { 
                    try { message = await response.text(); } catch {} 
                }
                
                if (window.showPopup) window.showPopup(message, 'error');
                return;
            }

            const modalEl = document.getElementById('employeeFormModal');
            bootstrap.Modal.getInstance(modalEl).hide();
            if (window.showPopup) window.showPopup(isUpdate ? 'Employee updated.' : 'Employee created.', 'success');
            if (window.loadEmployees) loadEmployees();
            
        } catch (err) {
            console.error(err);
            if (window.showPopup) window.showPopup('Unexpected error occurred.', 'error');
        } finally {
            btn.disabled = false;
            btn.textContent = originalText;
=======
                try {
                    const contentType = response.headers.get('Content-Type') || '';
                    if (contentType.includes('application/json')) {
                        const payload = await response.json();
                        if (payload && typeof payload === 'object' && payload.message) {
                            message = payload.message;
                        }
                    } else {
                        const text = await response.text();
                        if (text) message = text;
                    }
                } catch {
                    // ignore
                }

                if (window.showPopup) {
                    window.showPopup(message, 'error');
                }
                return;
            }

            if (window.showPopup) {
                window.showPopup(isUpdate ? 'Employee updated successfully.' : 'Employee created successfully.', 'success');
            }

            const modalEl = form.closest('.modal');
            bootstrap.Modal.getInstance(modalEl).hide();
            if (window.loadEmployees) loadEmployees();
        } catch (err) {
            if (window.showPopup) {
                window.showPopup('Unexpected error occurred while saving the employee.', 'error');
            }
            console.error(err);
>>>>>>> Mahmoud-V3
        }
    });
</script>
