What’s in your project right now (drivers + booking + payment)
Core entities (important fields)
RentACar.Core/Entities/Car.cs

CarId, ModelName, PlateNumber, PricePerDay, IsAvailable, CarImage, CategoryId

No “IsActive” field (your “active/inactive” is effectively IsAvailable right now).

RentACar.Core/Entities/Booking.cs

BookingId, CustomerId, CarId, Startdate, Enddate

BookingStatus (string), TotalPrice, Subtotal, PromocodeId

Driver-related:

HasDriver

DriverId (nullable)

DriverDailyFee (nullable, snapshot at booking time)

Pickup fields: PickupAddress, PickupLocationLabel, PickupLatitude/Longitude, PickupDateTime

RentACar.Core/Entities/Driver.cs

DriverId, EmployeeId, AspNetUserId, DriverCode, IsActive

Also stores FullName, Email, Phone (but you can read Name from Employee.Name and Email/Phone from AspNetUser via nav props)

RentACar.Core/Entities/Payment.cs

PaymentId, BookingId, Amount, PaymentStatus, PaymentMethod, TransactionId, PaidAt

RentACar.Core/Constants/StatusConstants.cs

Booking: Pending, Confirmed, InProgress, AwaitingReturn, Completed, Cancelled, Rejected
Payment: Pending, Paid, Cancelled, Refunded, Failed

DTOs / Managers / Repos involved
Booking DTOs

RentACar.Application/DTOs/BookingDto.cs contains MakeBookingRequestDto
(includes HasDriver — DriverDailyFee must NOT be accepted from client)

BookingListDto.cs used for ControlPanel Booking list
(missing HasDriver + DriverId fields currently)

Managers

BookingManager.MakeBookingAsync currently auto-assigns a driver and hardcodes fee 20/day if HasDriver==true.

Contract PDF generation in RentACar.Web/Controllers/BookingController.cs also hardcodes driver fee 20/day.

Browse bug

BrowseController uses CarManager.SearchCarsForListAsync / GetAvailableCarsInTimelineAsync without forcing IsAvailable==true, so inactive/unavailable cars can appear.

Customer “My Bookings”

Views/Bookings/MyBookings.cshtml shows car images in the list (you want them removed).

Details modal already shows only the chosen car photo (good).

Payments

Stripe checkout session is created with one total line item (no breakdown).

Receipt view Views/ControlPanel/Payment/Receipt.cshtml shows BookingSubtotal and discount, but no driver/extra fee lines.

Detailed implementation plan

(Single payment, auto-assign driver, editable later by admin/employee)

Phase 1 — Database + entity model updates
Add nullable car extra driver fee

Cars.extraDriverFeePerDay (nullable decimal(10,2)).

Add driver daily fee

Drivers.dailyFeePerDay (non-null decimal(10,2) with a default, e.g. 20.00 so old rows don’t break).

Update entities

Car.cs → public decimal? ExtraDriverFeePerDay { get; set; }
Driver.cs → public decimal DailyFeePerDay { get; set; }

Phase 2 — Pricing rules (single source of truth)

Create a single pricing calculation used by:

Booking creation

Driver reassignment (admin/employee)

PDF / receipt display

Rules:

baseRental = PricePerDay * rentalDays

driverService = DriverDailyFee * rentalDays

carExtra = Car.ExtraDriverFeePerDay * rentalDays (only if not null AND HasDriver==true)

subtotal = baseRental + driverService + carExtra

discount = subtotal * promo%

total = subtotal - discount

Important:
DriverDailyFee is a snapshot stored on Booking and does NOT change automatically if the driver changes later.

Phase 3 — Driver assignment at booking creation (AUTO-ASSIGN)
Edit BookingManager.MakeBookingAsync

If HasDriver == true:

Auto-assign a driver BEFORE payment

Select driver using:

IsActive == true

Available for full booking dates

Allowed to drive the car’s category

No conflicting bookings

If no driver available → fail booking creation (do not proceed to payment)

On successful assignment:

booking.DriverId = selectedDriverId

booking.DriverDailyFee = selectedDriver.DailyFeePerDay (snapshot)

Pricing includes:

base rental

driver service

car extra driver fee (if exists)

Trip creation:

Create Trip record immediately (TripStatus = Pending)

Payment behavior (single payment only)

Create Stripe Checkout Session immediately

Charge full total (car + driver + car extra fees)

No second payment, no deferred payment, no “pay later”

Security:

Ignore requestDto.DriverDailyFee (client must not set fees)

Phase 4 — Manual driver change in Booking Management (ADMIN / EMPLOYEE)

This phase stays, but behavior is adjusted to preserve single payment

Update Booking list API to include

HasDriver

DriverId

Update ControlPanel booking list UI (Index.cshtml)

Under status, show:

Green clickable “Assigned” if HasDriver==true && DriverId != null

(No “Has driver but not assigned” state anymore for customer bookings)

Click opens Driver Assignment partial.

Partial modal behavior (UPDATED, NOT REMOVED)

Shows driver details:

Name: driver.Employee.Name

Email + Phone: driver.User.Email, driver.User.PhoneNumber

If assigned:

Dropdown locked to current driver

Show “OK” button

Show small “Edit” button

On clicking Edit:

Dropdown unlocks

Shows Save / Cancel

On Save:

Validate new driver availability + category eligibility

Update booking.DriverId ONLY

DO NOT change booking.DriverDailyFee

DO NOT recalculate totals

Update Trip assignment

Send notification emails:

Old driver (optional: cancellation)

New driver

Customer (driver changed notice)

This guarantees price stability after payment, even if driver changes due to emergencies.

Phase 5 — My Bookings UI

Views/Bookings/MyBookings.cshtml:

Remove car thumbnails from booking list

Keep details modal unchanged

Payment button:

Always enabled for paid/unpaid bookings

No “waiting for driver assignment” state anymore

(Optional bandwidth optimization)

Remove carImage from booking list payload

Phase 6 — Fix browse “inactive cars displayed”

Unchanged from original plan:

Force IsAvailable == true

Filter Category.IsActive == true if applicable

Phase 7 — PDF / receipt updates
Contract PDF

Remove hardcoded 20/day

Use Booking.DriverDailyFee snapshot

Show:

Car rental

Driver service

Car extra driver fee (if applicable)

Discount

Total

Ticket PDF

Same breakdown.

Receipt view

Add rows for:

Driver service fee

Car extra driver fee

Totals must match Booking.Subtotal / Booking.TotalPrice exactly.

requirements.txt (Antigravity / Codex agent)
GOAL (UPDATED)

Implement auto driver assignment at booking creation, single-payment flow, driver fee snapshotting, and safe admin/employee driver replacement without price changes.

CORE RULES (UPDATED)

Single payment only (no deferred or second payments)

Driver must be assigned BEFORE payment

Driver fee is SNAPSHOTTED on booking

Changing driver NEVER changes totals

Partial view + status MUST remain

Acceptance Tests (UPDATED)
A) Car extra fee

Unchanged.

B) Driver fee

Admin sets driver.dailyFeePerDay

At booking creation:

booking.DriverDailyFee is snapshotted

Driver replacement does NOT change price

C) Auto assignment

Booking with HasDriver must fail if no driver available

Booking always has DriverId when paid

D) Driver replacement

Admin/employee can change driver

Booking status unchanged

Payment unchanged

Trip reassigned correctly

E) Browse + Emails

Unchanged, plus:

Email on driver reassignment (customer + new driver)

Final verdict (no sugar-coating)

This version is:

Financially correct

Operationally realistic

Stripe-safe

Defensible in a graduation review

Future-proof (no refund hell)

requirements.txt — Driver Allowed Categories + Pre-Payment Driver Availability + Auto-Assign

PROJECT: RentACar-LBDrive (ASP.NET Core MVC + EF Core)
GOAL: Add “driver can drive categories” feature + ensure driver availability is checked before payment and driver is auto-assigned to compute total cost.
SCOPE: DB + Entities + Repositories + Managers + Controllers + Views (Customer booking + Employee booking + Employee edit/add).
RULES:

Follow existing folder structure, naming, patterns (Managers/Repositories/DTOs/Views).

Do NOT hardcode driver fee (use Driver.DailyFeePerDay snapshot into Booking.DriverDailyFee).

Do NOT proceed to Stripe payment for “with driver” bookings unless a driver is successfully assigned.

Keep single payment flow (no second payment).

Do NOT break existing Employee/Customer booking flows.

1) DATABASE CHANGES (NEW TABLE)
1.1 Create table: DriverAllowedCategory (or DriverCarCategory)

Add a new table with attributes exactly:

Id INT (PK, Identity)

DriverId INT (FK -> Drivers.DriverId, NOT NULL)

CategoryId INT (FK -> Categories.CategoryId, NOT NULL)

Constraints:

UNIQUE (DriverId, CategoryId) to prevent duplicates.

ON DELETE CASCADE from Driver to mapping rows (recommended).

ON DELETE RESTRICT or CASCADE for Category (choose consistent with your schema; prefer RESTRICT if categories are stable).

Indexes:

IX_DriverAllowedCategory_DriverId

IX_DriverAllowedCategory_CategoryId

1.2 EF Core Migration

Add migration under RentACar.Infrastructure/Migrations

Name example: AddDriverAllowedCategories

2) ENTITY MODEL UPDATES
2.1 New entity in RentACar.Core/Entities

Create:

RentACar.Core/Entities/DriverAllowedCategory.cs
Properties:

int Id

int DriverId

int CategoryId
Navigation:

Driver Driver

Category Category (if Category entity exists in Core)

2.2 Update Driver entity

File: RentACar.Core/Entities/Driver.cs
Add navigation collection:

public ICollection<DriverAllowedCategory> AllowedCategories { get; set; } = new List<DriverAllowedCategory>();

2.3 Update Category entity (optional but recommended)

If Category entity exists:
Add:

public ICollection<DriverAllowedCategory> DriverAllowedCategories { get; set; }

2.4 DbContext mapping

File: RentACar.Infrastructure/Data/AppDbContext.cs (or your DbContext)

Add DbSet<DriverAllowedCategory>

Configure relationships + unique constraint (DriverId, CategoryId)

3) DTO + VIEWMODEL UPDATES
3.1 Driver DTOs

Update any Driver DTO used for admin/employee screens:
Add:

List<int> AllowedCategoryIds

If you use separate DTOs:

DriverCreateDto / DriverEditDto must include AllowedCategoryIds

3.2 Employee Edit/Add ViewModels

Where you edit employees and role can be Driver:

Add AllowedCategoryIds to the model passed to the view

Add Categories list (id + name) for selection UI (multi-select or checkbox list)

4) REPOSITORY LAYER
4.1 DriverAllowedCategoryRepository

Add repository in Infrastructure similar to your existing repos:
Methods required:

Task<List<int>> GetAllowedCategoryIdsByDriverIdAsync(int driverId)

Task SetAllowedCategoriesAsync(int driverId, List<int> categoryIds)

Must upsert:

Remove mappings not in list

Add missing mappings

Must enforce unique (DriverId, CategoryId)

Task<bool> DriverCanDriveCategoryAsync(int driverId, int categoryId)

4.2 Driver availability query must include category filter

Add method to DriverRepository or BookingRepository:

Task<List<Driver>> GetAvailableDriversForBookingAsync(
DateOnly start, DateOnly end, int categoryId)
Rules:

driver.IsActive == true

driver has full-day availability covering range (existing logic)

driver not conflicting (existing overlaps logic)

driver has mapping to categoryId in DriverAllowedCategory table

5) MANAGER LAYER (CORE LOGIC)
5.1 BookingManager — Pre-payment validation + assignment

File: RentACar.Application/Managers/BookingManager.cs

Update MakeBookingAsync (Customer and Employee flow must use the same logic):
If requestDto.HasDriver == true:

Determine car.CategoryId

Query available eligible drivers using repository method above

If none found:

Return failure response (do not create booking, do not create payment, do not redirect to Stripe)

Provide user-friendly error: “No drivers available for the selected dates/car category.”

Choose driver using deterministic tie-break:

Fewest upcoming active trips/bookings (asc)

Then DriverId (asc)
(Keep it simple; don’t invent rating system if you don’t have it)

Assign:

booking.DriverId = chosenDriver.DriverId

booking.DriverDailyFee = chosenDriver.DailyFeePerDay (snapshot)

Compute totals:

Include base rental + driver service + car extra driver fee (if any)

Create Trip (if your system uses Trip/DriverMission):

create Trip linked to booking and driver, status Pending

Proceed to Stripe checkout session for full total (single payment)

Security:

Ignore any fee values coming from client.

5.2 BookingManager — helper method

Add:

Task<List<DriverOptionDto>> GetAvailableDriversPreviewAsync(start, end, carId)
Used by UI to show “drivers available” count/list before customer proceeds.

6) CONTROLLER ENDPOINTS (Customer + Employee)
6.1 Customer booking page (/Views/Booking/Add)

When user selects:

dates

“with driver”
Do an AJAX call to endpoint to pre-check drivers (before payment):
Endpoint example (match your conventions):

GET /Booking/AvailableDrivers?carId=..&start=..&end=..
Response:

availableCount

optional: top 3 driver names (or just count; don’t expose private info if you prefer)

UI rules:

If availableCount == 0:

show error text

disable “Proceed/Pay” button

If > 0:

enable “Proceed/Pay”

show “Drivers available: X”

6.2 Employee “Add Booking” page

Employee must be able to book for customer:

with driver

without driver

Same behavior:

when with driver selected + dates selected → check available drivers

on submit → MakeBookingAsync auto-assigns driver before payment computation

employee booking payment flow should follow your existing employee logic (cash/pos/online) but totals must include driver if selected.

7) EMPLOYEE EDIT/ADD UI REQUIREMENTS (ROLE DRIVER)
7.1 Edit Employee modal/page

Requirement:

If role is Driver:

Role dropdown must show “Driver” selected

Driver-specific fields must be visible (existing driver fields + NEW categories selection)

Categories selection must be pre-populated from DriverAllowedCategory table

If role is not Driver:

Hide categories selection

Do not touch DriverAllowedCategory mappings

7.2 Add Employee modal/page

Requirement:

When selecting role “Driver” in the UI:

Show category selection (multi select / checkboxes)

It becomes required: must select at least 1 category

When saving:

Create/update Driver entity

Save mappings in DriverAllowedCategory table using SetAllowedCategoriesAsync

Validation:

If role is Driver and no categories selected → show validation error, block save.

8) BOOKING COST + PAYMENT CONSISTENCY
8.1 Cost computation

Must use one pricing function (single source of truth):
Inputs:

car.PricePerDay

dates (inclusive days)

hasDriver

booking.DriverDailyFee (snapshot)

car.ExtraDriverFeePerDay (nullable)

promo (optional)
Outputs:

subtotal, total + breakdown fields (optional)

8.2 Stripe

For HasDriver == true:

Do not create session unless driver assigned successfully

Charge full total once

9) ACCEPTANCE TESTS

A) Category filtering:

Driver with category A should never be assigned to car category B.

If only drivers of other categories exist, availability result must be 0 and booking must not proceed.

B) UI behavior:

Customer booking: selecting dates + with driver triggers availability check and blocks proceed if none.

Employee booking: same.

C) Booking creation:

With driver: Booking.DriverId is set at creation; Booking.DriverDailyFee is snapshot; Trip created.

Without driver: Booking.DriverId remains null; no trip created (if that’s your rule).

D) Payment:

With driver: payment includes driver service + optional car extra driver fee.

No second payment flow exists.

E) Edit employee:

If existing driver: categories show selected correctly.

If role changed away from driver: categories hidden; mappings untouched (or optionally removed ONLY if you explicitly want that—default: leave untouched).
