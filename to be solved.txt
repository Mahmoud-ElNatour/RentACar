Frontend and popups:
- dropdown background should be gray
- add show pass button
- check that the new popup is visible in all actions in all pages

Blacklist panel and inactive users:
- when unblacklist or setactive again in db it remains lockedout (fix) - fix design of lockedout page

Reset pass and email api
-reset pass should be random and send it by email
- implement email api (search for mailgun easy - create new acc in mailgun)

Customers and employees:
(Img high time and complexity)
- improve logic in which the DTO for the table not include the image and for change image here should load the imageDTO / same for creating customer, let it 2 pages the first took only the the other attr, and for image (document) to be desscussed if to be done from profile setting or from 2nd step signup.
--> better to be after login from profile settings (and give note in home page "please submit doc to verfy ur acc like the whish money APP) --> NEED TO CHANGE LOGIC CREATE CUSTOMER AND DTOS


Booking improvement:
- booking bug (if car is unavailable in some date range ca't book it in other range)
- when book dont use set_available false instead use other logic at booking to check the booking table if the car is booked in same date range
-bug in view booking details

Payments:
- in edit add apply promocode

Notes:
- for deleting we choose to don't delete if it has any action on system, instead we set it inactive
--> add attr to all needed tables (category,  promocode, paymentmethod).





Later on:
-request panel for canceling the booking from employee nso the admin can approve or deny
- refunded panel
